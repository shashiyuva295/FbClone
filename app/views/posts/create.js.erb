$("#post_like<%= @post.id%>").html("<%= escape_javascript(render partial: 'posts/show', locals: {post: @post}) %>");
// $(".post").text("<%= @post.likes.count %><%= (@post.likes.count) == 1 ? 'Like' : 'Likes'%>");
$("#post_like<%= @post.id%> .post").text('<%= @post.likes.count%>');
